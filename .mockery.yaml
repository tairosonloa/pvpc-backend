with-expecter: true
quiet: false
packages:
  pvpc-backend/internal:
    config:
      mockname: "{{.InterfaceName}}"
      filename: "{{.InterfaceNameSnake}}.go"
      outpkg: mocks
      dir: internal/mocks
    interfaces:
      PricesRepository:
      ZonesRepository:
